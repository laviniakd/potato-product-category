annotation_task_name: "Product Category Annotation"

# ── Server ────────────────────────────────────────────────────────────────────
server:
  port: 8000
  host: "0.0.0.0"     # bind to all interfaces so EC2 public IP works

# ── Data ─────────────────────────────────────────────────────────────────────
data_files:
  - data/annotations_input.jsonl

item_properties:
  id_key: "id"
  text_key: "image_url"   # POTATO requires a text_key; not displayed (image shown instead)

output_annotation_dir: "annotations/"
output_annotation_format: "json"

# ── Crowdsourcing parameters ──────────────────────────────────────────────────
annotation_per_instance: 3      # each image needs 3 independent annotations
instances_per_annotator: 25     # each worker sees 25 images

# ── UI ───────────────────────────────────────────────────────────────────────
instance_per_page: 1            # one image at a time
jumping_to_id_disabled: true    # workers can't skip to arbitrary items
hide_navbar: true               # cleaner crowdworker UI

html_layout: "templates/task.html"
base_html_template: "default"
site_file: "default"

# ── Prolific / user config ────────────────────────────────────────────────────
# Workers arrive via:
#   http://YOUR-EC2-IP:8000/?PROLIFIC_PID={%PROLIFIC_PID%}&STUDY_ID={%STUDY_ID%}&SESSION_ID={%SESSION_ID%}
user_config:
  allow_all_users: true
  login_type: "url_direct"
  url_argument: "PROLIFIC_PID"

# ── Annotation scheme ─────────────────────────────────────────────────────────
annotation_schemes:
  - annotation_type: radio
    name: product_category
    description: "What is the primary product category advertised in this ad?"
    labels:
      - "Dieting and Weight loss"
      - "Consumer Electronics"
      - "Pharmaceuticals"
      - "Consumer Packaged Goods and Durable Goods"
      - "Fitness Activities and Sporting Goods"
      - "Finance and Insurance"
      - "Food and Beverage Services"
      - "Health and Medical Services"
      - "Retail"
      - "Debated Sensitive Social Issue"
      - "Sexual Health"
      - "Home and Garden Services"
      - "Travel and Tourism"
      - "Gifts and Holiday Items"
      - "Clothing and Accessories"
      - "Weapons and Ammunition"
      - "Tobacco"
      - "Real Estate"
      - "Business and Industrial"
      - "Non-Profits"
      - "Collectables and Antiques"
      - "Alcohol"
      - "Personal/Consumer Telecom"
      - "Media"
      - "Family and Parenting"
      - "Politics"
      - "Computer Software"
      - "Cosmetic Services"
      - "Legal Services"
      - "Religion and Spirituality"
      - "Metals"
      - "Culture and Fine Arts"
      - "Oil and Gas"
      - "Pet Ownership"
      - "Events and Performances"
      - "Vehicles"
      - "Education and Careers"
      - "City or Region"
    required: true
    horizontal: false
